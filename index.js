import React from 'react'; // importing react dependencies
import ReactDOM from 'react-dom'; // importing react dom dependencies
import App from './App'; // importinf app from app.js

ReactDOM.render(<App />, document.getElementById('root')); // rendering App on to html page with id root 